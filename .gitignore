# Internal project working files — kept local, never published
CLAUDE.md
memory/
.claude/

# MASTER RULE: never publish ANY markdown files (feedback, notes, specs,
# questions, briefs). They stay local only — do not commit or push .md.
*.md

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Dropbox sync artifacts
*conflicted copy*
*.tmp
